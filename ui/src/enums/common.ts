export enum DeviceType {
  Mobile = 'Mobile',
  Desktop = 'Desktop'
}

export enum ValidType {
  Application = 'application',
  Dataset = 'dataset',
  User = 'user'
}

export enum ValidCount {
  Application = 5000,
  Dataset = 5000,
  User = 100000000
}
